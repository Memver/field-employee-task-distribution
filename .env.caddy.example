# Caddy + Let's Encrypt on host (no Docker). Copy into .env — see caddy-local.md.

DOMAIN=example.com
ACME_EMAIL=admin@example.com

FRONTEND_HOST=https://dashboard.example.com
BACKEND_CORS_ORIGINS="https://dashboard.example.com"
